import { defineBackground } from "wxt/sandbox";

export default defineBackground(() => {
    // Background wiring — the sync WebSocket, alarms (snapshots/GC/compaction),
    // context menus, and keyboard commands — is implemented in later phases.
});
